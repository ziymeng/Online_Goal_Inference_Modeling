{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: Flask-SocketIO Server",
            "type": "python",
            "request": "launch",
            "program": "${workspaceFolder}/server.py",
            "console": "integratedTerminal",
            "env": {
                "FLASK_ENV": "development"
            }
        }
    ]
}
